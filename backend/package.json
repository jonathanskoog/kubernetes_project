{
  "name": "backend",
  "version": "1.0.0",
  "description": "This service handles the database operations for the application.",
  "main": "index.js",
  "scripts": {
    "dev": "npx prisma migrate deploy && nodemon src/index.ts",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2"
  }
}
